# 本地 Spec

本模板不包含 XHH 代码规范，也不包含项目硬件或产品事实。

它仅用于阻止 Trellis 初始化时自动生成通用的 `backend/`、`frontend/` 和 `guides/` Spec。XHH MCU 的目录、接口和源码规则由全局 `xhh-mcu-development` Skill 提供。

只有用户明确确认某条规则属于当前项目本地特例、且不应进入全局 Skill 时，才在本目录新增文档。不得复制全局 Skill 的规则。
